module.exports = {
  semi: true, // ใช้ ; ปิดบรรทัด
  singleQuote: true, // ใช้ single quote
  trailingComma: "all", // ใส่ comma หลังสุดทุกกรณี
  printWidth: 100, // ความกว้างสูงสุดต่อบรรทัด
  tabWidth: 2, // ขนาด tab 2 ช่อง
  endOfLine: "auto", // ปรับ line ending อัตโนมัติ
  bracketSpacing: true, // ใส่ space ใน object literal { foo: bar }
  bracketSameLine: false, // ปิด tag > บนบรรทัดเดียวกับ JSX
  arrowParens: "always", // ใส่วงเล็บเสมอใน arrow function
  singleAttributePerLine: true, // JSX attribute แต่ละตัวบนบรรทัดใหม่
  jsxSingleQuote: true, // ใช้ single quote ใน JSX ด้วย
};
